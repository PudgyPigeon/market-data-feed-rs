{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust App",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build",
                    "--bin=market-data-feed-rs",
                ]
            },
            "program": "${workspaceFolder}/target/debug/market-data-feed-rs",
            "args": ["-r", "input.txt"], 
            "cwd": "${workspaceFolder}"
        }
    ]
}